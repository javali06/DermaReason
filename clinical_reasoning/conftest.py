# Empty on purpose: its presence at the project root makes pytest add this
# directory to sys.path, so tests can do `from src.knowledge_base import ...`
# regardless of the current working directory pytest is invoked from.
